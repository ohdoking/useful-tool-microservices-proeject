server:
  port: 8082

spring:
  application:
    name: explainservice
  cloud:
    config:
      uri: http://localhost:8888

endpoints:
  refresh:
    enabled: true

management:
  #security:
  # enabled: false
  endpoints:
   web:
    exposure:
     include: "*"